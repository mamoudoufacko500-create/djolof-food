import type { IconType } from "react-icons/lib";

export type IconsItems ={
    id:number;
    icon: IconType;
    path:string;
}

export type ListProductType ={
  id: number;
  name:string;
  image:string;
  prix:number;
  desc:string;
  note:number;
}

